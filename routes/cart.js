/* const controllers = require("../controllers/");

router.get("/products", controllers.getProducts); //Obtiene prod de la DB
router.get("/products-cart", controllers.getProductsCart); //Trae prod de la DB
router.post("/products-cart", controllers.addProductCart); //Agrega prod al cart
router.put("/products-cart/:productId", controllers.putProduct); //Modifica cantidad de prod en el cart
router.delete("/products-cart/:productId", controllers.deleteProduct); //Elimina prod del cart

module.exports = router */